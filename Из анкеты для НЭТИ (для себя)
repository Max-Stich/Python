# (Из анкеты для НЭТИ, со сложными условиями) - Дан НЕИЗВЕСТНЫЙ массив ПОЛОЖИТЕЛЬНЫХ ЧИСЕЛ, который вводит пользователь.
Найти в массиве максимальное значение, которое не превышает число 20.

# Код

lst = [] 	# будущий пользовательский массив
с = 'старт'
b = ['стоп', 'Стоп', 'СТОП']
while с not in b: 	# Создаём массив
    c = input("Введите значение. По окончанию ввода напишите 'стоп' ")
    lst.append(c)
    if c in b:
        break	# если ввод равен b, заканчиваем цикл
res = min(lst)
del lst[-1]	# убираем слово стоп (всегда последнее значение) из массива
result = int(res)	# переводим str переменной res в int

for i in lst:	# идём по списку
    if int(i) > result and int(i) <= 20:	# и находим подходящие значения
        result = int(i)		# записываем значение в результат в типе int

print(result)

# нужен рефакторинг и проверки


## РЕФАКТОРИНГ И ДОП ПРОВЕРКИ

Условия: дан НЕИЗВЕСТНЫЙ массив НЕИЗВЕСТНОЙ длины, ЛЮБЫХ ЦЕЛЫХ ЧИСЕЛ и СИМВОЛОВ, который вводит пользователь.
Написать программу, которая находит наибольшее числовое значение в массиве, которое не превышает число 20.
Если в массиве нет числа  меньше 20ти, вывести строку "Нет значений меньше 20ти"
Заметка: В массиве должно быть хотя бы одно число

lst = []        # Основной пользовательский массив
с = ''          #   Начальная точка формирования массива через While
print()
print('Введите список значений. По окончанию ввода напишите "стоп"', end='\n')      # Информаирование пользователя

while с.lower() not in ['стоп']:        #   Формируем список, пока пользователь не введёт "стоп"
    c = input()
    if c.lower() == 'стоп':         #   Сразу проверяем на завершение
        stop_it = (input('Закончить ввод? Введите "Да" или "Нет": '))
        if stop_it.lower() == 'да':         # Если завершение НЕ ошибочное - завершаем работу
            break
        elif stop_it.lower() == 'нет':          # Если завершение ошибочное, продолжаем
            c = input()
    minus = []          # Отладочный список для проверки на отрицательное число
    minus.extend(c)         
    for i in range(33,48):          # Проверяем вхождение знака с индексом 0 в таблицу ASCII
        if с == chr(i):         #   Если входит - пропускаем значение
            continue
    if minus[0] == '-' and len(minus) != 1:         # Если не входит в ASCII и в minus больше 1ого значения - значит в вводе отрицательное число
        lst.append(c)       # Добавляем его в основной список
    elif c.isdigit() == True:       # Если в вводе нет минуса, проверяем что введённое значение - точно число (не строка и не символ)
        lst.append(c)       # И добавляем в основной список
    else:           # Если ни одно из условий не сработало (ввод - строка)
        continue        # Пропускаем и возвращаемся к вводу

if len(lst) > 1:        # Если в lst значений больше 1ого
    result = int(min(lst))     # то берём min-значение
elif len(lst) == 1:     # Если в lst одно значение - выводим и завершаем программу
    print('В списке одно числовое значение: ', *lst)
else:       # Если в lst нет числовых значений - завершаем
    print('В списке нет числовых значений')

if len (lst) > 1:       # если в lst больше одного значения
    for i in lst:
        if int(i) > result and int(i) <= 20:        # проверяем основной список на главные условия
            result = int(i)
    if result > 20:
        print('Нет значений меньше 20ти')
    else:
        print('Максимальное число в списке, которое меньше числа 20:', result)


## ТЕПЕРЬ нужно добавить проверку на числа с плавающей точкой, проверку на ВСЕ символы доступные на клавиатуре и
 # Потом сделать рефакторинг и оптимизацию.

###############################################
